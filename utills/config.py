import os
import sys

class Tokens():
    

    token= (str(os.getenv('token')))

    TokenLastFM= (str(os.getenv('TokenLastFM')))

    tenor_key= (str(os.getenv('tenor')))

    Genius_key= (str(os.getenv('Genius_key')))

    Weather_key= (str(os.getenv('weather')))

    ksoft_keys= (str(os.getenv('ksoft_keys')))

    osu_Token = (str(os.getenv('osu_Token')))

    SDC_key = (str(os.getenv('SDC_Key')))

    Prefix = ";"